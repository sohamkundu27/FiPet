# FiPet

## Starting the Frontend

1. Move to FiPetFrontend.

    ```bash
    cd FiPetFrontend
    ```

2. Install dependencies

   ```bash
   npm install
   ```

3. Start the app (locally)

   ```bash
   npx expo start
   ```
